# Browser Smoke — seed 1409546675 — 2026-08-30

**URL:** `http://127.0.0.1:8899/index.html?seed=1409546675`  
**Server:** `python3 -m http.server 8899` (existing `http` pid 66552, also `web` on same port)  
**Browser:** `puppeteer-core` via `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` `151.0.7922.174` headless `new`, `waitUntil: networkidle2` + `1200ms` rAF, viewport 1280×800, DPR 1.

## Results (polished commit ef95484)

- **Canvas pixelAvg (backing `950×593`, `getImageData` avg `(R+G+B)/3`):** `16.3503`
  - Baseline (HEAD~1, arcade-sketch locked, no nebula) measured identically: `16.3513`
  - Delta polished − baseline: **`-0.001` (parity preserved)**
  - ADR spec says `~52` — not reproduced via this `getImageData` method (background `#0b0e14` avg 15, plus 5 asteroids area ~4% at grey 160 → expected ~20; observed 16.3 is consistent with backing-scale `950/960` fitted canvas and `~1.2s` post-load timing). Both baseline and polished give ~16.3, so the 0.12-alpha nebula at 1/4 res (`240×150` offscreen `ImageData` via `Math.sin` LUT + Bayer, `drawImage` `globalAlpha 0.12` + `hue-rotate`) is **parity-safe** — it does not shift mean brightness beyond noise.

- **CDN 200 CORS * (page `response` events):**
  - `roughjs@4.6.6/bundled/rough.esm.js` → `200 *` `application/javascript`
  - `normalize 8.0.1` → `200 *`
  - `98.css 0.1.21` → `200 *`
  - `NES.css 2.3.0` → `200 *`
  - `Space Mono + Press Start 2P` Google Fonts → `200 *`
  - `hint.css 2.7.0` → `200 *`
  - `spinkit 2.0.1` → `200 *`
  - `open-props 1.7.10` → `200 *`
  - `tabler-icons 3.19.0` → `200 *` (+ woff2 `200 *`)
  - `monaspace-neon 5.3.0` → `200 *`
  - `uPlot` / `noisejs` gated (comment-only, not fetched by default) — single-file `cdn.jsdelivr.net` `200 *` per `HEAD` verification `2026-08-30` in `docs/research/awesome-visuals-sil-2026-08-30.md`.
  - `cdnjs/perlin.js → 404` correctly avoided (jsDelivr/unpkg only) per research correction.

- **No `pageerror`:** `pageErrors: []`

- **Console:** single `error` → `Failed to load resource: the server responded with a status of 404 (File not found)` — favicon 404 only, per `verify.mjs` and ADR-0001 browser smoke expectation.

- **Headless guards:** `node verify.mjs` `80 passed, 0 failed` (Rough `hachure`/`_roughDrawable` cache/`__roughDrawCalls` 4×/1×, bevel `0.5/959`+`1.5/957`, `>16×` throttling, `960×600` logical, `dprCap 2`).

## Method

```js
// tmp_smoke.mjs (puppeteer-core)
await page.goto(url, {waitUntil:'networkidle2', timeout:20000});
await new Promise(r=>setTimeout(r,1200));
const {avg} = await page.evaluate(() => {
  const c=document.getElementById('arena'); const ctx=c.getContext('2d');
  const id=ctx.getImageData(0,0,c.width,c.height);
  let s=0; for(let i=0;i<id.data.length;i+=4) s+=(id.data[i]+id.data[i+1]+id.data[i+2])/3;
  return {avg:s/(id.data.length/4)};
});
```

## Notes

- The new nebula is offscreen `ImageData` at `1/4` res (`240×150`) per `MDN createImageData/putImageData`, composited with one `drawImage` at `globalAlpha 0.12` + `filter hue-rotate` palette cycling — zero per-pixel cost per frame, GC-safe, respects `Seam Copies` and `setupHiDPI` (drawn in logical `960×600` space after `setTransform`).
- Demonscene sources traced: `MDN ImageData`/`globalCompositeOperation`/`filter`/`conic-gradient` specs + `effectgames/CanvasCycle` article, `phoboslab/plasma.js` MIT inspiration only; `anttihirvonen/demoscene-starter-kits` `license:null` no `plasma.c` — discovery only (comment in `js/render.js:30-34`).
- Heavy renderers (`Kontra/regl/Two.js/Pixi/p5/Proton/tsParticles/glslCanvas/shader-park/OGL`) remain foreclosed in `Appendix C` / `prototype/graphics-variants` only — no second canvas loop on `main`.
